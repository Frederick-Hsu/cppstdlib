#ifndef LIST_HPP
#define LIST_HPP

namespace MyContainers
{
    template<typename ElemType, typename Allocator> class list;
    template<typename ElemType, typename Allocator> class list_base;
    template<typename ElemType>                     class list_node;
    template<typename ElemType>                     class list_node_base;
    template<typename ElemType, typename Allocator> class list_impl;
}

#endif  /* LIST_HPP */
